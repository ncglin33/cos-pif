{
  "scripts": {
    "build": "echo 'No build step required'",
    "start": "npx serve -s ."
  },
  "dependencies": {
    "google-auth-library": "^10.5.0",
    "googleapis": "^170.0.0",
    "nodemailer": "^7.0.12"
  }
}
